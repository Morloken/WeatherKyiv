# This app uses Open-Meteo API which is free and requires no API keys!
# No environment variables needed for this application.

# Optional: Backend server port (for local development only)
# PORT=3001
